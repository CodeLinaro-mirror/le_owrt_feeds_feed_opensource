diff --git a/ChangeLog-CVS b/ChangeLog-CVS
index 47b54cea..646f2762 100644
--- a/ChangeLog-CVS
+++ b/ChangeLog-CVS
@@ -4820,7 +4820,7 @@
 2005-11-06  Steve Langasek <vorlon@debian.org>
 
 	* modules/pam_env/pam_env.c: don't treat a missing
-	/etc/environment as a fatal error when attempting to read it,
+	/etc/pam_environment as a fatal error when attempting to read it,
 	and try to read this file by default; this restores the behavior
 	from Linux-PAM 0.76.
 
diff --git a/NEWS b/NEWS
index ca436baf..09b7fd0f 100644
--- a/NEWS
+++ b/NEWS
@@ -405,7 +405,7 @@ Release 0.99.2.0
 * pam_xauth: Look for xauth executable in multiple places
 * pam_unix: Disable user check in unix_chkpwd only if real uid
   is 0 (CVE-2005-2977). Log failed password check attempt.
-* pam_env: Support /etc/environment again, but don't treat it as
+* pam_env: Support /etc/pam_environment again, but don't treat it as
   error if it is missing.
 * pam_userdb: Fix memory leak.
 
diff --git a/modules/pam_env/pam_env.8.xml b/modules/pam_env/pam_env.8.xml
index 75ff862b..bf5e752f 100644
--- a/modules/pam_env/pam_env.8.xml
+++ b/modules/pam_env/pam_env.8.xml
@@ -59,7 +59,7 @@
       option.
     </para>
     <para>
-      Second a file (<filename>/etc/environment</filename> by default) with simple
+      Second a file (<filename>/etc/pam_environment</filename> by default) with simple
       <emphasis>KEY=VAL</emphasis> pairs on separate lines will be read.
       With the <emphasis>envfile</emphasis> option an alternate file can be specified.
       And with the <emphasis>readenv</emphasis> option this can be completely disabled.
@@ -230,7 +230,7 @@
         </listitem>
       </varlistentry>
       <varlistentry>
-        <term><filename>/etc/environment</filename></term>
+        <term><filename>/etc/pam_environment</filename></term>
         <listitem>
           <para>Default environment file</para>
         </listitem>
diff --git a/modules/pam_env/pam_env.c b/modules/pam_env/pam_env.c
index f5f8cead..343b7f44 100644
--- a/modules/pam_env/pam_env.c
+++ b/modules/pam_env/pam_env.c
@@ -6,7 +6,7 @@
  * template for this file (via pam_mail)
  */
 
-#define DEFAULT_ETC_ENVFILE     "/etc/environment"
+#define DEFAULT_ETC_ENVFILE     "/etc/pam_environment"
 #define DEFAULT_READ_ENVFILE    1
 
 #define DEFAULT_USER_ENVFILE    ".pam_environment"
diff --git a/modules/pam_env/pam_env.conf.5.xml b/modules/pam_env/pam_env.conf.5.xml
index fca046fe..24e7b527 100644
--- a/modules/pam_env/pam_env.conf.5.xml
+++ b/modules/pam_env/pam_env.conf.5.xml
@@ -62,7 +62,7 @@
     </para>
 
     <para>
-      The <filename>/etc/environment</filename> file specifies
+      The <filename>/etc/pam_environment</filename> file specifies
       the environment variables to be set. The file must consist of simple
       <emphasis>NAME=VALUE</emphasis> pairs on separate lines.
       The <citerefentry><refentrytitle>pam_env</refentrytitle><manvolnum>8</manvolnum></citerefentry>

--- a/ChangeLog    2024-03-13 12:00:00
+++ b/ChangeLog    2024-03-13 12:00:00
@@ -5736,1 +5736,1 @@
-	pam_env: Document the /etc/environment file.
+	pam_env: Document the /etc/pam_environment file.
@@ -5740,1 +5740,1 @@
-	Add a paragraph about /etc/environment. Add environ(7) reference.
+	Add a paragraph about /etc/pam_environment. Add environ(7) reference.

--- a/modules/pam_env/pam_env.8	2024-03-13 12:00:00
+++ b/modules/pam_env/pam_env.8	2024-03-13 12:00:00
@@ -46,1 +46,1 @@
-Second a file (/etc/environment
+Second a file (/etc/pam_environment
@@ -143,1 +143,1 @@
-/etc/environment
+/etc/pam_environment

--- a/modules/pam_env/README	2024-03-13 12:00:00
+++ b/modules/pam_env/README	2024-03-13 12:00:00
@@ -15,1 +15,1 @@
-Second a file (/etc/environment by default) with simple KEY=VAL pairs on
+Second a file (/etc/pam_environment by default) with simple KEY=VAL pairs on

--- a/modules/pam_env/pam_env.conf.5	2024-03-13 12:00:00
+++ b/modules/pam_env/pam_env.conf.5	2024-03-13 12:00:00
@@ -51,1 +51,1 @@
-/etc/environment
+/etc/pam_environment

--- a/modules/pam_env/Makefile.am
+++ b/modules/pam_env/Makefile.am
@@ -11,1 +11,1 @@
-dist_man_MANS = pam_env.conf.5 pam_env.8 environment.5
+dist_man_MANS = pam_env.conf.5 pam_env.8 pam_environment.5

--- a/modules/pam_env/Makefile.am
+++ b/modules/pam_env/Makefile.am
@@ -31,1 +31,1 @@
-dist_sysconf_DATA = environment
+dist_sysconf_DATA = pam_environment

--- a/modules/pam_env/Makefile.am
+++ b/modules/pam_env/Makefile.am
@@ -35,1 +35,1 @@
-environment.5: pam_env.conf.5.xml
+pam_environment.5: pam_env.conf.5.xml

diff --git a/modules/pam_env/environment.5 b/modules/pam_env/pam_environment.5
similarity index 100%
rename from modules/pam_env/environment.5
rename to modules/pam_env/pam_environment.5

diff --git a/modules/pam_env/environment b/modules/pam_env/pam_environment
similarity index 100%
rename from modules/pam_env/environment
rename to modules/pam_env/pam_environment
