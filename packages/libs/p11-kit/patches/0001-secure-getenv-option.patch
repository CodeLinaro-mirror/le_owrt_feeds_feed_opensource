From c8fbe9604b2e18c8c7682d47d13d57148b37fc1c Mon Sep 17 00:00:00 2001
From: Abderraouf Boukhatem <abderraouf.boukhatem_ext@softathome.com>
Date: Wed, 29 Jan 2025 11:49:04 +0100
Subject: [PATCH] Added option to disable secure_getenv

---
 common/compat.c   | 5 +++++
 meson.build       | 4 ++++
 meson_options.txt | 4 ++++
 3 files changed, 13 insertions(+)

diff --git a/common/compat.c b/common/compat.c
index 6b39a7b..23109bd 100644
--- a/common/compat.c
+++ b/common/compat.c
@@ -908,8 +908,13 @@ _p11_getauxval (unsigned long type)
 char *
 secure_getenv (const char *name)
 {
+#ifdef P11_ENABLE_SECURE_ENV
 	if (_p11_getauxval (AT_SECURE))
+#else
+	if (getauxval (AT_SECURE) && strncmp (name, "P11_", 4))
+#endif
 		return NULL;
+
 	return getenv (name);
 }
 
diff --git a/meson.build b/meson.build
index 7bdcdff..264f07f 100644
--- a/meson.build
+++ b/meson.build
@@ -371,6 +371,10 @@ conf.set10('HAVE_DECL_REALLOCARRAY',
            cc.has_header_symbol('stdlib.h', 'reallocarray',
                                prefix: '#define _GNU_SOURCE'))
 
+p11_enable_secure_getenv = get_option('enable_secure_getenv')
+if p11_enable_secure_getenv
+  conf.set('P11_ENABLE_SECURE_ENV', 1)
+endif
 # --------------------------------------------------------------------
 # libffi
 
diff --git a/meson_options.txt b/meson_options.txt
index c1f2362..dbe6806 100644
--- a/meson_options.txt
+++ b/meson_options.txt
@@ -93,3 +93,7 @@ option('rpc_min', type : 'integer',
 option('rpc_max', type : 'integer',
        min : 0, max : 2, value : 2,
        description : 'Maximum RPC protocol version we support')
+
+option('enable_secure_getenv', type : 'boolean',
+       value : false,
+       description : 'Disable access to environemnet variables if not prefixed with P11 and secure execution is required')
-- 
2.43.0

