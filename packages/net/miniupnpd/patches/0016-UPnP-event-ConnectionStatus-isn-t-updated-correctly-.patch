From a32cc79cf999f4fe780677f8fd40b02981e85c87 Mon Sep 17 00:00:00 2001
From: sahbot <support.opensource@softathome.com>
Date: Thu, 6 Jun 2024 15:32:57 +0200
Subject: [PATCH 16/22] UPnP event: ConnectionStatus isn't updated correctly
 (HOP-3580)[change]

Problem: When losing WAN the ConnectionStatus event isn't sent correctly
When IPv6 isn't start at boot IPv6 is disable and reboot is needed loosing
notify socket

Cause: Miniupnpd is using interface addr to know if WAN is lost

Solution: Use amx to verify WAN status when configuration is reloaded
Do not disable IPv6, since we use so_bindtodevice to prevent having to
restart

Issue: HOP-3580 - [CDROUTER][UPnP] ConnectionStatus value in NOTIFY event is
NOT correctly updated after WAN connectivity Loss
GitOrigin-RevId: f68e2147e30aca560e8419be3f4b90a00f03b159
---
 miniupnpd/amx/amxmisc.c    | 64 ++++++++++++++++++++++++++++++++++++++
 miniupnpd/amx/amxmisc.h    |  1 +
 miniupnpd/amx/amxpinhole.c |  2 +-
 miniupnpd/configure        |  8 +++--
 miniupnpd/getconnstatus.c  | 10 ++++++
 miniupnpd/miniupnpd.c      |  7 +++++
 6 files changed, 89 insertions(+), 3 deletions(-)

diff --git miniupnpd/amx/amxmisc.c miniupnpd/amx/amxmisc.c
index eedbece..f57307b 100644
--- miniupnpd/amx/amxmisc.c
+++ miniupnpd/amx/amxmisc.c
@@ -62,16 +62,43 @@
 
 #include <string.h>
 #include <syslog.h>
+#include <netmodel/client.h>
 #include <amxc/amxc.h>
 #include <amxc/amxc_macros.h>
 #include <amxp/amxp.h>
 #include <amxd/amxd_object.h>
+#include <amxd/amxd_path.h>
 #include <amxb/amxb.h>
 #include "config.h"
 #include "../upnpglobalvars.h"
 
 static amxb_bus_ctx_t* bus_ctx = NULL;
 
+static void init_loglevel(void) {
+    amxc_var_t ret;
+    amxc_var_t arg;
+    uint32_t level = 200;
+
+    amxc_var_init(&arg);
+    amxc_var_init(&ret);
+    amxc_var_set_type(&arg, AMXC_VAR_ID_HTABLE);
+    amxb_call(bus_ctx, "UPnP.", "list_trace_zones", &arg, &ret, 1);
+    if(amxc_var_type_of(&ret) != AMXC_VAR_ID_NULL) {
+        level = GETP_UINT32(&ret, "0.upnp");
+    }
+    if(level >= 500) {
+        setlogmask(LOG_UPTO(LOG_DEBUG));
+    } else if(level >= 400) {
+        setlogmask(LOG_UPTO(LOG_INFO));
+    } else if(level >= 300) {
+        setlogmask(LOG_UPTO(LOG_NOTICE));
+    } else {
+        setlogmask(LOG_UPTO(LOG_WARNING));
+    }
+    amxc_var_clean(&ret);
+    amxc_var_clean(&arg);
+}
+
 int init_amx(void) {
     int r = -1;
     if(bus_ctx != NULL) {
@@ -80,12 +107,15 @@ int init_amx(void) {
         r = amxb_be_load(ubus_backend_url);
         if(r == 0) {
             r = amxb_connect(&bus_ctx, ubus_sock_url);
+            netmodel_initialize();
+            init_loglevel();
         }
     }
     return r;
 }
 
 void shutdown_amx(void) {
+    netmodel_cleanup();
     amxb_disconnect(bus_ctx);
     bus_ctx = NULL;
     return;
@@ -145,4 +175,38 @@ exit:
     amxc_var_clean(&ret_a);
     amxc_var_clean(&ret_b);
     return rv;
+}
+
+int amx_is_wan_up(void) {
+    int rv = -1;
+    amxc_var_t ret;
+    amxc_var_t wan_obj;
+    amxc_string_t wan_ip_path;
+    amxd_path_t path;
+
+    amxc_var_init(&ret);
+    amxc_var_init(&wan_obj);
+    amxc_string_init(&wan_ip_path, 0);
+    amxd_path_init(&path, "Device.Logical.Interface.[Alias=='wan'].X_PRPL-COM_WAN.");
+    rv = amxb_resolve(bus_ctx, &path, &ret);
+    when_failed_status(rv, exit, syslog(LOG_ERR, "Couldn't resolve Device.Logical.Interface.[Alias=='wan'].X_PRPL-COM_WAN. to a fix path (%d)", rv));
+    when_false(amxc_var_type_of(&ret) != AMXC_VAR_ID_NULL, exit);
+    rv = amxb_get(bus_ctx, GETI_CHAR(&ret, 0), 0, &wan_obj, 1);
+    when_failed_status(rv, exit, syslog(LOG_ERR, "Couldn't get Logical interface %s data (%d))", GETI_CHAR(&ret, 0), rv));
+    rv = -1;
+    amxc_string_setf(&wan_ip_path, "0.'%s'.IPv4Address", GETI_CHAR(&ret, 0));
+    if(strcmp(GETP_CHAR(&wan_obj, amxc_string_get(&wan_ip_path, 0)), "") != 0) {
+        rv = 0;
+    }
+    amxc_string_setf(&wan_ip_path, "0.'%s'.IPv6Address", GETI_CHAR(&ret, 0));
+    if(strcmp(GETP_CHAR(&wan_obj, amxc_string_get(&wan_ip_path, 0)), "") != 0) {
+        rv = 0;
+    }
+exit:
+    syslog(LOG_INFO, "amx_is_wan_up: %d (%s)", rv, GETI_CHAR(&ret, 0));
+    amxc_string_clean(&wan_ip_path);
+    amxc_var_clean(&ret);
+    amxc_var_clean(&wan_obj);
+    amxd_path_clean(&path);
+    return rv;
 }
\ No newline at end of file
diff --git miniupnpd/amx/amxmisc.h miniupnpd/amx/amxmisc.h
index 3f93263..a78e443 100644
--- miniupnpd/amx/amxmisc.h
+++ miniupnpd/amx/amxmisc.h
@@ -71,6 +71,7 @@ void shutdown_amx(void);
 int init_amx(void);
 amxb_bus_ctx_t* get_amx_bus_ctx(void);
 int are_addresses_from_same_host(const char*, const char*);
+int amx_is_wan_up(void);
 
 #endif
 
diff --git miniupnpd/amx/amxpinhole.c miniupnpd/amx/amxpinhole.c
index 7689c46..0ea8eb5 100644
--- miniupnpd/amx/amxpinhole.c
+++ miniupnpd/amx/amxpinhole.c
@@ -175,7 +175,7 @@ int add_pinhole(UNUSED const char* ifname,
     return rv;
 }
 
-int delete_pinhole(uint32_t uid) {
+int delete_pinhole(uint16_t uid) {
     int rv = -1;
     amxc_string_t alias;
     amxc_var_t args;
diff --git miniupnpd/configure miniupnpd/configure
index aa172b4..4081a5e 100755
--- miniupnpd/configure
+++ miniupnpd/configure
@@ -437,7 +437,9 @@ case $OS_NAME in
 		;;
 	OpenWrt)
 		OS_URL=http://www.openwrt.org/
-		echo "#define USE_IFACEWATCHER 1" >> ${CONFIGFILE}
+		if [ 1"$FW" != 1"amx" ] ; then
+			echo "#define USE_IFACEWATCHER 1" >> ${CONFIGFILE}
+		fi
 		GETIFADDRS=1
 		if [ -z "$FW" ] ; then
 			FW=iptables
@@ -598,7 +600,9 @@ case $FW in
 		echo "# generated by $0 on `date`" > config.mk
 		echo "SRCDIR = ${BASEDIR}" >> config.mk
 		echo "CPPFLAGS += -I." >> config.mk
-		echo "LDLIBS += -lamxc -lamxp -lamxd -lamxb -lamxo" >> config.mk
+		echo "LDLIBS += -lsahtrace -lamxc -lamxp -lamxd -lamxb -lamxo -lnetmodel" >> config.mk
+		echo 'STAGING_LIBDIR = $(if $(STAGING_DIR), -L$(STAGING_DIR)/lib) $(if $(STAGING_DIR), -L$(STAGING_DIR)/usr/lib)' >> config.mk
+		echo 'LDFLAGS += $(STAGING_LIBDIR)' >> config.mk
 		;;
 	nftables)
 		MAKEFILE=Makefile.linux_nft
diff --git miniupnpd/getconnstatus.c miniupnpd/getconnstatus.c
index 332784c..6cde725 100644
--- miniupnpd/getconnstatus.c
+++ miniupnpd/getconnstatus.c
@@ -8,8 +8,13 @@
 #include <stdio.h>
 #include <sys/types.h>
 #include <netinet/in.h>
+#include <syslog.h>
+#include "config.h"
 #include "getconnstatus.h"
 #include "getifaddr.h"
+#ifdef USE_AMX
+#include "amx/amxmisc.h"
+#endif
 
 #define STATUS_UNCONFIGURED (0)
 #define STATUS_CONNECTING (1)
@@ -30,6 +35,10 @@
 int
 get_wan_connection_status(const char * ifname)
 {
+#ifdef USE_AMX
+	(void) ifname;
+	return (amx_is_wan_up() == 0 ? STATUS_CONNECTED : STATUS_DISCONNECTED);
+#else
 	char addr[INET_ADDRSTRLEN];
 	int r;
 
@@ -37,6 +46,7 @@ get_wan_connection_status(const char * ifname)
 	 * I'm afraid it should be device specific */
 	r = getifaddr(ifname, addr, INET_ADDRSTRLEN, NULL, NULL);
 	return (r < 0) ? STATUS_DISCONNECTED : STATUS_CONNECTED;
+#endif
 }
 
 /**
diff --git miniupnpd/miniupnpd.c miniupnpd/miniupnpd.c
index 80b752a..659af49 100644
--- miniupnpd/miniupnpd.c
+++ miniupnpd/miniupnpd.c
@@ -2187,6 +2187,9 @@ main(int argc, char * * argv)
 			     " " MINIUPNPD_GIT_REF
 #endif
 			     " " __DATE__ );
+#ifdef USE_AMX
+			puts("using amx backend");
+#endif
 #ifdef USE_PF
 			puts("using pf backend");
 #endif
@@ -2372,8 +2375,12 @@ main(int argc, char * * argv)
 				       ipv6_addr_for_http_with_brackets);
 			} else {
 				memcpy(ipv6_addr_for_http_with_brackets, "[::1]", 6);
+#ifdef USE_AMX
+				syslog(LOG_WARNING, "no HTTP IPv6 address, not disabling IPv6");
+#else
 				syslog(LOG_WARNING, "no HTTP IPv6 address, disabling IPv6");
 				SETFLAG(IPV6DISABLEDMASK);
+#endif
 			}
 		}
 #endif	/* ENABLE_IPV6 */
-- 
2.34.1

