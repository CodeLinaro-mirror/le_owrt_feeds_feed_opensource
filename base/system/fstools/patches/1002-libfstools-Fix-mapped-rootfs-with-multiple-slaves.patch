Index: fstools-2022-06-02-93369be0/libfstools/common.c
===================================================================
--- fstools-2022-06-02-93369be0.orig/libfstools/common.c
+++ fstools-2022-06-02-93369be0/libfstools/common.c
@@ -157,8 +157,10 @@ char *block_get_slave(const char *bdev)
 		goto out;
 
 	while ((d = xreaddir(dirp))) {
-		if (++slaves > 1)
-			goto out;	/* more than one slave */
+		if (++slaves > 1) {
+			ULOG_WARN("More than one slave => take the first one");
+			break; /* more than one slave */
+		}
 		strncpy(slave, d->d_name, sizeof(slave));
 		slave[sizeof(slave) - 1] = '\0'; /* make sure slave is always NULL terminated */
 	}
@@ -168,5 +170,5 @@ char *block_get_slave(const char *bdev)
 		closedir(dirp);
 	if  (fd >= 0)
 		close(fd);
-	return slaves == 1 ? slave : NULL;
+	return slaves > 0 ? slave : NULL;
 }
